import { StyleSheet } from 'react-native'

const maxWidth: string = '100vw'

export default StyleSheet.create({
    homeContainer: {
        position: 'relative',
        flex: 1,
        zIndex: 400
    }
})